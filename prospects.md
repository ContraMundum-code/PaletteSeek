# PaletteSeek 优化方向

本文档用于集中记录当前版本的局限、候选优化方向和团队讨论意见。

> 以下内容均为候选方案，不代表已经实现。欢迎大家直接补充、修改，并在文末留下意见。

## 当前重点：自然语言语义检索

### 现状与问题

目前系统的核心检索信号仍以关键词、标签和 HEX 色值为主。虽然已经加入分词、同义词和口语映射等基础的自然语言处理，但仍存在以下问题：

- 用户通常不知道系统内部有哪些标签。
- 用户很难直接给出准确色值。
- 同一种需求有很多口语表达，规则词表难以全部覆盖。
- `适合答辩、正式但不要太压抑的深蓝色配色` 这类完整意图，容易被拆解得不准确或不充分。
- 持续增加同义词规则会提高维护成本，但语义覆盖仍然有限。

因此，更值得探索的方向不是继续要求用户适应系统标签，而是让系统主动理解用户语言，并与作品元数据对齐。

### 候选方案：建立作品语义档案

根据每件作品已有的关键词、标签、色值、色彩比例和其他元数据，从多个角度生成较细致的文本描述，例如：

- **视觉特征**：主要颜色、冷暖、明暗、饱和度和色彩比例。
- **情绪气质**：沉稳、克制、轻快、活泼、怀旧或理性等感受。
- **风格倾向**：现代、装饰性、科技感、艺术感或商务感。
- **适用场景**：答辩 PPT、学术汇报、品牌设计、海报、数据展示等。
- **口语化描述**：正式但不沉闷、清爽有活力、有质感但不过分厚重等。
- **不适合的需求**：不偏甜美、不适合热烈节庆场景等反向特征。

示例：

```text
作品元数据：
深蓝、灰蓝、低饱和、沉稳、理性、现代艺术、视觉传达

生成的语义描述：
这是一组以深蓝和灰蓝为主的低饱和配色，整体沉稳、克制且专业，
适合学术答辩、商务汇报、科技展示和数据型 PPT。
视觉上正式但不过分压抑，不偏活泼、甜美或节庆风格。
```

### 候选检索流程

```text
作品关键词、色值及其他元数据
        ↓
生成多角度作品描述
        ↓
将作品描述建立为语义索引
        ↓
用户输入自然语言需求
        ↓
计算用户需求与作品描述的语义相似度
        ↓
融合现有标签匹配、排除条件和颜色相似度
        ↓
返回结果并解释匹配原因
```

这一方案不是完全替换现有检索，而是形成互补：

- 结构化标签保证事实准确。
- 色值检索保证颜色匹配精度。
- 作品描述扩大自然语言覆盖范围。
- 语义匹配负责连接用户表达与内部数据。
- 原有筛选和排序逻辑继续提供可控性。

### 可选实施方式

| 方式 | 优点 | 局限 |
|---|---|---|
| 模板生成描述 | 免费、稳定、容易解释，不会随意编造 | 语言覆盖和表达丰富度有限 |
| 大模型离线批量生成 | 描述自然，能覆盖更多口语和使用场景 | 需要模型或 API，并需检查生成事实 |
| 模板打底后由模型扩写 | 兼顾事实准确和语言覆盖 | 实现流程和质量检查稍复杂 |
| 本地文本向量模型匹配 | 运行时无需在线 API，适合语义检索 | 首次需下载模型，增加依赖和部署体积 |
| 在线文本向量或大模型服务 | 接入较快，通常效果较好 | 需要网络、密钥、费用和隐私评估 |

### 推荐的渐进路径

1. 先统一和检查现有作品标签，避免错误元数据被放大。
2. 使用模板为作品生成可核验的多角度描述。
3. 人工检查一部分作品，评估描述质量和区分度。
4. 准备一组真实自然语言查询及理想结果，作为评测集。
5. 条件允许时，引入中文文本向量模型进行语义匹配实验。
6. 将语义分数与现有颜色、标签和排除条件融合，而不是直接替换原检索。
7. 在前端展示匹配原因和系统理解结果，保证检索可解释。

### 需要重点验证的问题

- 不同作品生成的描述是否有足够区分度。
- 模型是否会根据颜色推断出元数据中不存在的用途或情绪。
- 否定表达能否正确处理，例如“不压抑”“不要红色”。
- 程度表达能否合理处理，例如“稍微活泼”“偏正式”。
- 语义相似是否会压过用户明确指定的颜色要求。
- 模型和索引是否能在当前部署环境稳定运行。
- 优化后的结果是否确实优于现有关键词和规则映射。

## 其他可讨论方向

| 优化方向 | 简要说明 | 状态 |
|---|---|---|
| 检索结果解释 | 展示命中的颜色、情绪、风格、场景及放宽条件 | 待讨论 |
| 硬条件与软偏好 | 区分“必须深蓝”和“最好有科技感” | 待讨论 |
| 否定和程度理解 | 改进“不压抑”“不要太暗”“稍微活泼”等表达 | 待讨论 |
| 检索效果评测 | 建立自然语言查询与理想结果的小型测试集 | 待讨论 |
| 标签质量检查 | 合并重复标签，修正含义模糊或粒度不一致的标签 | 待讨论 |
| 用户反馈闭环 | 记录结果是否有帮助，用于调整映射和排序 | 待讨论 |

## 团队意见

请在下表中继续补充建议；如有新的方向，也可以直接新增章节。

| 提议人 | 建议或疑问 | 优先级 | 结论 |
|---|---|---|---|
|  |  |  | 待讨论 |
